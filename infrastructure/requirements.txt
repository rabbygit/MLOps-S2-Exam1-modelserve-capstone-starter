# Pulumi infrastructure dependencies. Lives in infrastructure/.venv so it
# doesn't tangle with the project's host venv (mlflow/feast/etc.).
#
# Bootstrap (once per fresh clone):
#     cd infrastructure
#     python -m venv .venv && source .venv/bin/activate
#     pip install -r requirements.txt

# Core SDK. pulumi-aws 6.66 needs pulumi >= 3.142, so use a range.
pulumi>=3.142.0,<4.0.0

# AWS provider. Wraps the AWS Terraform provider under the hood.
pulumi-aws>=6.66.0,<7.0.0
