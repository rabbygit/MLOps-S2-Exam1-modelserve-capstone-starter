# Web framework
fastapi==0.110.0
uvicorn[standard]==0.27.1
gunicorn==21.2.0
pydantic==2.6.1

# ML stack
scikit-learn==1.4.1.post1
pandas==2.2.0
numpy==1.26.4
pyarrow==15.0.0

# MLflow tracking + registry (used by both the server and the FastAPI client)
mlflow==2.11.1
boto3==1.34.59
# mlflow 2.11 still imports pkg_resources, which setuptools 81+ removed
setuptools<81

# Feature store (the [redis] extra installs the Redis online-store driver)
feast[redis]==0.40.0

# Prometheus metrics (Session 3-4 will scrape /metrics; we register the metrics now)
prometheus-client==0.20.0

# Postgres driver — MLflow backend store
psycopg2-binary==2.9.9

# Loads .env into os.environ for host-side scripts (train.py, materialize_features.py).
# Containers get their env from compose's `environment:` block, not from this.
python-dotenv==1.0.1

# Tests
pytest==8.0.0
httpx==0.27.0
