# Web framework
fastapi==0.110.0
uvicorn[standard]==0.27.1
gunicorn==21.2.0
pydantic==2.6.1

# ML stack
scikit-learn==1.4.1.post1
pandas==2.2.0
numpy==1.26.4
pyarrow==15.0.0

# MLflow tracking + registry. Used by the server image and host scripts.
mlflow==2.11.1
boto3==1.34.59
# mlflow 2.11 still imports pkg_resources, which setuptools 81+ removed.
setuptools<81

# Feature store. The [redis] extra installs the online-store driver.
feast[redis]==0.40.0

# Prometheus metrics (registered now, scraped from S3-4 onward).
prometheus-client==0.20.0

# Postgres driver for MLflow backend store.
psycopg2-binary==2.9.9

# Loads .env for host-side scripts (train.py, materialize_features.py).
# Containers get env from compose's `environment:` block, not from this.
python-dotenv==1.0.1

# Tests
pytest==8.0.0
httpx==0.27.0
